<h1>Dashboard</h1>

<div class="dashboard">
  <div class="row stats">
    <div class="span3">
      <h2>Your Mileage</h2>
      <div class="number positive">12</div>
    </div>
    <div class="span3">
      <h2>Your Position</h2>
      <div class="number positive">1st</div>
    </div>
    <div class="span3">
      <h2>Teams Mileage</h2>
      <div class="number positive">104</div>
    </div>
    <div class="span3">
      <h2>Teams Position</h2>
      <div class="number positive">3rd</div>
    </div>
  </div>

  <div class="row rides">
    <div class="span3 form">
      <h3>Add A Ride</h3>
      <form>
        <label>Date<input type="text" /></label>
        <label>Distance<input type="number" /></label>
        <label>Description<input type="text" /></label>

        <div class="btn-group" data-toggle="buttons-radio">
          <button type="button" class="btn active">Round Trip</button>
          <button type="button" class="btn">One Way</button>
        </div>

        <button type="submit" class="btn btn-orange pull-right">Submit</button>
      </form>
    </div>

    <% rides = Ride.first(8) %>
    <% if rides.blank? %>
      <div class="span9">
        <p><%= t("ride.index.none") %></p>
      </div>
    <% else %>
      <table class="span9 rides table table-striped table-bordered table-hover">
        <thead>
          <tr>
            <th>Ride Date</th>
            <th>Ride Distance</th>
            <th>Total Miles</th>
            <th>Total Trips</th>
          </tr>
        </thead>
        <% rides.each do |ride| %>
          <%= content_tag_for :tr, ride do %>
            <td><%= link_to ride.date.strftime("%b #{ride.date.day.ordinalize}"), ride %></td>
            <td><%= ride.distance%></td>
            <td>Coming Soon</td>
            <td>Coming Soon</td>
          <% end %>
        <% end %>
      </table>
    <% end %>

  </div>

</div>