<%= simple_form_for @ride, builder: EmbeddedFormBuilder do |f| %>
  <%= f.input :date, as: :datepicker %>
  <%= f.input :distance %>
  <%= f.input :description, input_html: { rows: 2 } %>

  <%= f.hidden_field :is_round_trip %>
  <%= f.button_group :is_round_trip do %>
    <%= f.toggle_button t("ride.options.round"), true, active: true %>
    <%= f.toggle_button t("ride.options.one"), false %>
  <% end %>

  <%= f.button :button, t("ride.submit"), class: "btn btn-orange pull-right" %>
  <div class="clearfix"></div>
<% end %>

<script type="text/javascript">
  $("#is_round_trip button").click(function(){
    $("#ride_is_round_trip").val($(this).data("value"));
  });
</script>
