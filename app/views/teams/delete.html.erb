<% title "#{@team.name} - " + t("team.delete.title") %>

<h1><%= t("team.delete.title")%></h1>

<p>
  Are you sure you wish to delete the team '<%= @team.name %>'?
  Deleting it will delete all data associated with it, logged rides.
</p>

<%= link_to t("team.delete.action"), @team, data: { method: :delete }, class: "btn btn-danger" %>
<%= link_to "Cancel", :back, class: "btn" %>
