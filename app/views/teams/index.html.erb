<% title t("team.index.action") %>
<div class="row">
  <div class="span8">
    <h1><%= t("team.index.action") %></h1>
  </div>
  <div class="span4">
    <% if can? :create, Team %>
      <%= link_to t("team.add.action"), new_team_path, class: "btn btn-orange pull-right" %>
    <% end %>
    <div class="clearfix"></div>
  </div>
</div>

<%= render "teams/table", :teams=>@teams%>